meta {
  name: Login Account
  type: http
  seq: 4
}

post {
  url: {{local-url}}/api/v1/auth/login/
  body: json
  auth: inherit
}

body:json {
  {
      "email": "nova.izumi@example.com",
      "password": "Skyline#2042"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
